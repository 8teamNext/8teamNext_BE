tags:
  - name: Root
summary: API 상태 확인
description: API 서버 상태 및 문서 안내를 반환합니다.
produces:
  - application/json
responses:
  200:
    description: 서버 정상 동작
    schema:
      type: object
      properties:
        message:
          type: string
          example: "AI기반 개발자 취업 역량 분석 서비스인 Next입니다. Visit /docs for documentation."
